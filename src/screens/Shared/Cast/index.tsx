import React from 'react';

import {
  FAB,
  // CastButton
} from './styles';

const Cast = () => {
  return (
    <FAB
      style={{}}
      icon={() => {
        // return <CastButton />;
      }}
      onPress={() => {}}
    />
  );
};

export default Cast;
